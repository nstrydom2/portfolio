pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // Build the Java project using Maven
              sh 'mvn clean install'
            }
        }
    }
}
 
