pipeline {
    agent any

    environment {
        function_name = 'java-sample'
    }

    stages {

        // CI Start
        stage('Build') {
            steps {
                echo 'Build'
                sh 'mvn package'
            }
        }