pipeline {
  agent any
  
  environment {
    function_name = 'aws-1-pepiline'
  }
  
  stages {
    stage('Build') {
      steps {
        // Build the Java project using Maven
        sh 'mvn clean package'
      }
    }
  }
}